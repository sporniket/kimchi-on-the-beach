#ifndef TYPES_FOR_INPUT_OUTPUT_HPP
#define TYPES_FOR_INPUT_OUTPUT_HPP

// standard includes
#include <cstdint>

// esp32 includes

// project includes

/** @brief Direction for io pins.
 */
typedef enum {
    READ,
    WRITE
} PinDirection ;

#endif